const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      className="searchbar"
      style={{
        marginBottom: "20px",
        padding: "10px",
        fontSize: "16px",
      }}
    />
  );
};
